# ── Backend ─────────────────────────────────────────────────────
APP_ID=                   # Application ID
APP_SECRET=jJKjtlbE38Yqb9lDazZBD7KdImoF3TWgF1w              # Application secret (used for JWT signing)

# ── Database ───────────────────────────────────────────────────
DATABASE_URL=postgresql://postgres.ijcavxnozqxypbjsyzeq:woxUeiSRkGAtJDz1@aws-1-us-west-2.pooler.supabase.com:6543/postgres

# ── Frontend (exposed to browser via Vite) ──────────────────────
VITE_APP_ID=              # Application ID

# ── Backend (Auth) ─────────────────────────────────────────────
BASE_URL=http://localhost:4173                 # Base URL for email links (e.g. http://localhost:5173)

# ── Admin Role ──────────────────────────────────────────────────
ADMIN_EMAILS=scott@sisaacson.io# Comma-separated list of emails that get admin role on signup
ADMIN_SECRET_PATH=/admin/login  # Secret admin sign-in URL path

# ── Email (Resend) ──────────────────────────────────────────────
RESEND_API_KEY=re_RrSi54mF_11VKQ5T2Rv9aEoTc8HqxdGHR          # Resend API key (optional in dev; logs link if omitted)
FROM_EMAIL=scott@sisaacson.io               # Sender address, e.g.
# ── Moonshot API ────────────────────────────────────────────────
MOONSHOT_API_KEY=         # Moonshot API key for admin-configured AI features

# ── Stripe (optional — payment integration is mocked by default) ─
STRIPE_PUBLISHABLE_KEY=   # Stripe publishable key (pk_...)
STRIPE_SECRET_KEY=        # Stripe secret key (sk_...)
STRIPE_WEBHOOK_SECRET=    # Stripe webhook endpoint secret (whsec_...)
STRIPE_PRICE_STARTER=     # Stripe Price ID for Starter plan
STRIPE_PRICE_PRO=         # Stripe Price ID for Pro plan
STRIPE_PRICE_PREMIUM=     # Stripe Price ID for Premium plan
